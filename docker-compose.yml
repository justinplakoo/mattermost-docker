version: "3"

services:

  db:
    build: db
    read_only: true
    restart: unless-stopped
    volumes:
      - /opt/data/containers/mattermost/db/13/data:/var/lib/postgresql/data
      - /etc/localtime:/etc/localtime:ro
    environment:
      - POSTGRES_USER=mmuser
      - POSTGRES_PASSWORD=mmuser_password
      - POSTGRES_DB=mattermost

  app:
    build:
      context: app
      args:
        - edition=team
        - PUID=1000
        - PGID=1000
    depends_on:
      - db
    restart: unless-stopped
    volumes:
      - /opt/data/containers/mattermost/plugins:/plugins:ro
      - /opt/data/containers/mattermost/app/mattermost/config:/mattermost/config:rw
      - /opt/data/containers/mattermost/app/mattermost/data:/mattermost/data:rw
      - /opt/data/containers/mattermost/app/mattermost/logs:/mattermost/logs:rw
      - /opt/data/containers/mattermost/app/mattermost/plugins:/mattermost/plugins:rw
      - /opt/data/containers/mattermost/app/mattermost/client-plugins:/mattermost/client/plugins:rw
      - /etc/localtime:/etc/localtime:ro
      # When you want to use SSO with GitLab, you have to add the cert pki chain of GitLab inside Alpine
      # to avoid Token request failed: certificate signed by unknown authority (link: https://github.com/mattermost/mattermost-server/issues/13059)
      # - <path_to_your_gitlab_pki>/pki_chain.pem:/etc/ssl/certs/pki_chain.pem:ro
    environment:
      # set same as db credentials and dbname
      - MM_USERNAME=mmuser
      - MM_PASSWORD=mmuser_password
      - MM_DBNAME=mattermost

      # use the credentials you've set above, in the format:
      # MM_SQLSETTINGS_DATASOURCE=postgres://${MM_USERNAME}:${MM_PASSWORD}@db:5432/${MM_DBNAME}?sslmode=disable&connect_timeout=10
      - MM_SQLSETTINGS_DATASOURCE=postgres://mmuser:mmuser_password@db:5432/mattermost?sslmode=disable&connect_timeout=10

      # in case your config is not in default location
      #- MM_CONFIG=/mattermost/config/config.json

  web:
    build: web
    depends_on:
      - app
    ports:
      - "8040:80"
    read_only: true
    restart: unless-stopped
    volumes:
      - /etc/localtime:/etc/localtime:ro
